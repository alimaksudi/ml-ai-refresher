# Control Card

This project evaluates deterministic regex and schema controls; it does not train or
call a language model. Fixed versioned outputs make the evaluation reproducible. The
candidate policy cannot infer intent, resist every paraphrase, validate open-text claims,
or authorize external actions. Production controls require identity, permissions,
least privilege, audited tools, incident response, and domain-specific review.
