# Model Card

The lab uses the repository's two-layer decoder-only `TinyLanguageModel` with a fixed
character tokenizer. It is trained locally on all three prompt formats, then frozen.
Its purpose is to expose conditioning and controlled comparison mechanics. It is not
a capable general chat model and must not support production, safety, or broad prompt
quality claims.
