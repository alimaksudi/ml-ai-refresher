# Core — needed to build and run every notebook
numpy>=1.24
matplotlib>=3.7
nbformat>=5.7

# Classical ML and ML engineering (Sections 02-03)
scikit-learn>=1.3
scipy>=1.10
pandas>=2.0
xgboost>=2.0
shap>=0.44

# Deep learning (Section 04)
torch>=2.1

# NLP / LLM / RAG (Sections 05-06) — used only in the relevant notebooks
# Install the relevant optional profile instead of expanding the core environment:
# - requirements-nlp-rag.txt
# - requirements-agents.txt
# - requirements-evaluation-production.txt

# Curriculum validation and notebook execution
nbclient>=0.10
ipykernel>=6.29
